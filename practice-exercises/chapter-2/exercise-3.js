// 3. Convert the string "19.5" to a number, add 0.5, and print the result.

const numericString = "19.5";
const convertedNumber = Number(numericString);
const result = convertedNumber + 0.5;

console.log(result); // Output: 20